name: Publish package

on:
  push:
    tags: ['v*']

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: npm
      # Node 22 bundles npm 10, which can sign provenance via OIDC but cannot
      # authenticate a publish via OIDC. Trusted publishing needs npm >= 11.5.1.
      - run: npm install -g npm@latest
      - run: npm ci
      - run: npm run check
      - name: Verify tag matches package version
        run: |
          PKG_VERSION="$(node -p "require('./package.json').version")"
          echo "package.json=v$PKG_VERSION tag=$GITHUB_REF_NAME"
          test "v$PKG_VERSION" = "$GITHUB_REF_NAME"
      - run: npm run pack:check
      - run: npm publish --provenance
